# Implementation

In this section, we present:
1. All of our training code including 8 `.ipynb` notebooks for both ResNet, DenseNet, ResNet-SCConv, DenseNet-SCConv on CIFAR and Food dataset
2. The Core source code of the plugin SCConv module in pure python code